[tool.poetry]
name = "digdir-api"
version = "0.0.1"
description = ""
authors = ["pbencze <paul.bencze@nav.no>"]

[tool.poetry.dependencies]
python = "^3.7"
fastapi = "^0.52.0"
python-dotenv = "^0.13.0"
concepttordf = "^1.0.0-rc.7"
datacatalogtordf = "1.0.1"
uvicorn = "^0.11.3"
requests = "^2.23.0"



[tool.coverage.paths]
source = ["src", "*/site-packages"]

[tool.coverage.run]
branch = true
source = ["deetly"]

[tool.coverage.report]
show_missing = true
fail_under = 100

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
